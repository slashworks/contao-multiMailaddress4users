contao-multiMailaddress4users
=============================

Simple dca override that allows one email adress for multiple backend users.

Just copy the module into your contao modules path system/modules
